# AI collaboration — raw running log

Written by the AI agent immediately after each phase. Unedited. Accuracy over tone.

## Phase 1 — Foundation   (2026-09-09 ~04:10 +07:00)
- Decisions I made that the spec did not dictate:
  - Module path `github.com/hodynguyen/service-scheduler` (derived from the git remote).
  - `go 1.24` in go.mod rather than 1.22: current pgx/testcontainers releases require >= 1.23/1.24
    and the brief says "1.22+". Local toolchain is 1.26; Docker build image is `golang:1.26-alpine`.
  - Runtime image is distroless static. Migrations and seed run inside the app on boot
    (`APP_SEED=true`) instead of a separate migrate container, to keep the quickstart at two commands.
  - `.hody/` (workflow tool state) lives in the parent directory, not in this repo; added to
    `.gitignore` anyway in case someone runs the tool from inside the repo.
  - Package layout: `internal/{domain,service,repository/postgres,http,observability}` + `cmd/server`.
    Migrations are SQL files embedded from `internal/repository/postgres/migrations`.
- Alternatives I considered and rejected, with reasons:
  - golang-migrate CLI/library for migrations: rejected, adds a dependency and a second binary for a
    handful of files. A ~60-line embedded runner with a `schema_migrations` table is enough here.
  - `testify`: rejected to keep dependencies minimal; stdlib `testing` is verbose but sufficient.
  - `google/uuid`: rejected for now; IDs are generated by Postgres (`gen_random_uuid()`) and the
    UUID syntax check in handlers is a regex. Will revisit if it becomes awkward.
- Where I was uncertain or guessing:
  - Whether the reviewer wants `.hody/` artefacts committed. I guessed no (tooling noise).
  - Whether `golang:1.26-alpine` exists on the reviewer's registry mirror. It exists on Docker Hub
    as of my knowledge; if the build fails, lower to `golang:1.25-alpine`.
- What I could not verify myself (needs human check):
  - Nothing yet; `docker compose up` is verified in the final step.
- Anything I got wrong first and had to correct:
  - Nothing in this phase.
