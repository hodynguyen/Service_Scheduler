// Package postgres is the pgx-based repository. It owns the schema
// (embedded migrations), seed data and the booking transaction. The
// database — not this package — is the correctness guarantee for INV-1..3.
package postgres
